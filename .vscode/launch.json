{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run npm start",
            "type": "node-terminal",
            "request": "launch",
            "command": "npm run start",
            "cwd": "${workspaceFolder}"
        }
    ]
}